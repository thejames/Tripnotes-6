<%= form_with(model: note_category, local: true) do |form| %>
  <% if note_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note_category.errors.count, "error") %> prohibited this note_category from being saved:</h2>

      <ul>
        <% note_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
