<% flash.each do |name, msg| %>
  <div class="alert alert-<%= name == :notice ? "danger" : "success" %>" role="alert">
    <a class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>
